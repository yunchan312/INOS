{
  "name": "@inos/desktop",
  "version": "0.0.1",
  "private": true,
  "description": "INOS 데스크톱 셸 — 배포된 웹앱을 렌더링",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && electron .",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^33.0.0",
    "typescript": "^5.4.0"
  }
}
