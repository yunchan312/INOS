---
title: INOS 발제문 생성 프롬프트 스펙
version: 1.4
updated: 2026-08-25
status: draft
---

# INOS 발제문 생성 프롬프트 스펙

인문학 모임(영화/책)용 AI 발제문 생성 규격. 모델은 Claude Sonnet 단일.

## 전제

- **회원들은 이미 작품을 감상한 상태로 모인다.** 함께 보지 않는다
- 회원은 4명 안팎, 초대제 지인 기반
- **기억의 한계는 고려하지 않는다.** 내용을 다 기억한다고 간주한다
- 회원들은 **평범한 독자이자 영화 감상자**다. 어느 쪽에서도 전문 어휘를 갖고 있지 않다
- **발제문은 모임 당일 그 자리에서 처음 본다.** 미리 배포하지 않는다
- 완독·완주는 참가자 책임이다. 미완독자를 배려해 질문을 무디게 만들지 않는다

여기서 규칙 하나가 따라 나온다 — **사전 준비를 요구하는 질문은 쓰지 않는다.**
세어보라, 찾아보라, 확인해보라는 요구는 전부 여기 걸린다. 당일에 처음 보는
질문이니 미리 해올 수가 없다.

반대 방향의 배려도 하지 않는다. 회원이 그 장면을 떠올릴 수 있을지 걱정해서
질문을 뭉뚱그리지 않는다. 세밀한 디테일을 물어도 된다.

---

## 0. 생성 파이프라인

```
[1단계] 팩트시트 생성        검색 ON   · 저가 모델 가능
        ↓ 메타데이터 검증 실패 시 → FAILED + 수정 폼
[2단계] 관찰 사실 추출        검색 OFF
        ↓
[3단계] 발제문 6문항 생성     검색 OFF
        ↓
[4단계] 자기검증 채점         저가 모델
        ↓ 실패 시 재생성
```

**검색을 켜는 단계는 1단계뿐이다.** 3단계에서 검색을 켜면 웹의 해석 글이
컨텍스트에 들어오고, 모델이 남의 결론을 그대로 베껴 온다. 그러면
"답을 말하지 말고 볼 곳만 준다"는 원칙이 구조적으로 무너진다.

### 입력 파라미터

| 파라미터    | 값                                  | 비고                    |
| ----------- | ----------------------------------- | ----------------------- |
| `workType`  | `film` \| `fiction` \| `nonfiction` | 비문학은 별도 슬롯 세트 |
| `factSheet` | 1단계 산출물                        | 검증된 외부 사실만      |
| `hostNotes` | 그룹장이 직접 적은 관찰 메모 (선택) | 있으면 2단계보다 우선   |

`hostNotes`는 모델이 가장 약한 지점을 사람으로 메우는 자리다. 웹에는 줄거리와
해석만 있고 구도·사운드는 거의 없기 때문에, 작품을 실제로 본 그룹장이 한두 줄만
적어주면 `technique` 슬롯의 품질이 통째로 달라진다. 없으면 2단계 결과로 진행하되
`technique`이 `null`로 떨어질 확률이 올라간다.

1단계 팩트시트에서는 **형식에 관한 서술만** 뽑는다 (촬영·편집·음향 관련 언급,
제작 방식, 형식적 특징). 해석과 결론은 버린다 — 그게 들어오면 정답 유출이 된다.

`element`는 파라미터가 아니다. 모델이 2단계에서 뽑은 관찰 사실을 보고 **가장 두껍게
잡히는 요소 하나를 스스로 고른다**. 고른 값은 출력에 실어 무엇을 다뤘는지 남긴다.

---

## 1. 슬롯 구성 (영화·책 동일)

| #   | 슬롯                 | 목적                    | 필수 필드                                           |
| --- | -------------------- | ----------------------- | --------------------------------------------------- |
| 1   | `warmup`             | 방의 온도를 올린다      | —                                                   |
| 2   | `technique`          | 요소 하나를 눈에 보이게 | `element`(모델 선택), `observation` (책: `locator`) |
| 3   | `thematic`           | 주제/해석               | `angle`                                             |
| 4   | `thematic`           | 주제/해석 (다른 각도)   | `angle`                                             |
| 5   | `stance`             | 작가·감독의 세계관      | `evidence`, `source`                                |
| 6   | `private_experience` | 각자의 삶으로 착지      | —                                                   |

`workType`이 바꾸는 것은 두 가지뿐이다 — `technique`이 참조할 요소 목록(영화 6 /
책 5)과 팩트시트. 프롬프트 템플릿과 검증 로직은 하나를 공유한다.

### 출력 스키마

```json
{
  "questions": [
    {
      "slot": "technique",
      "element": "sound_design",
      "observation": "귀를 막고 싶어지는 장면들의 음원은 대부분 일상적인 사물이다",
      "locator": null,
      "angle": null,
      "evidence": null,
      "source": null,
      "question": "..."
    }
  ]
}
```

---

## 2. 슬롯별 가이드

### 1) `warmup`

정보 획득이 아니라 **전원이 한 번씩 입을 여는 것**이 전부다.

- 작품 지식 없이 답할 수 있어야 한다. 감정·인상·기억이면 충분
- **오답이 존재하면 안 된다.** 여기서 한 명이 "틀린 말"을 하면 남은 다섯 문항 동안 입을 닫는다
- 해석을 요구하지 않는다. 해석은 3번부터

문형: `"…한 순간이 있었나요? 언제였습니까?"` / `"…중 가장 오래 남은 것은?"`

```
❌ 이 작품의 주제는 무엇이라고 생각하시나요?   → warmup이 아니라 thematic
❌ 어떻게 보셨나요?                          → 너무 넓어 아무도 먼저 말하지 않음
✅ 보는 동안 머리보다 몸이 먼저 반응한 순간이 있었나요?
```

### 2) `technique`

6개 슬롯 중 유일하게 **가르치는** 질문.

- `element`는 모델이 고른다. 단 **한 문항에 하나만.** 두 요소를 섞은 질문은 초점이
  흩어져서 아무것도 안 보이게 만든다
- 고르는 기준은 "이 작품에서 가장 두드러지는 요소"다. 관찰 사실이 빈약한 요소를
  억지로 고르면 `observation`이 뭉뚱그려지고, 그러면 질문 전체가 죽는다
- **`technique`은 nullable이다.** 어느 요소에서도 화면/지면 수준의 관찰 사실이
  안 나오면 `null`로 두고 `thematic` 하나로 대체한다. 줄거리로 우회한 가짜
  기법 질문보다 없는 편이 낫다 (`stance`와 같은 처리)
- `observation`은 **관찰 가능한 사실**이어야 한다. 해석이 섞이면 그게 곧 정답 유출
- **기법 용어를 질문문에 쓰지 않는다. 영화든 책이든 마찬가지다.** "사운드 디자인"이라는
  말 없이 사운드를 묻고, "시점"이나 "화자" 없이 시점을 묻는다. 회원은 평범한
  감상자이지 비평가가 아니다
- **숙제를 내지 않는다.** "몇 번인지 세어보세요", "다시 확인해보세요" 같은 요구는
  영화든 책이든 금지다. 당일에 처음 보는 질문이라 아무도 답할 수 없다.
  세는 대신 **떠올리게** 한다
- 책은 `locator`(대목 지시) 필수. **원문을 그대로 인용하지 않는다.** 모델은 본문
  텍스트를 갖고 있지 않으므로, 인용을 요구하면 지어낸다. 쪽수도 넣지 않는다 —
  판본마다 다르다. 대신 "어머니가 강에서 발견되는 대목"처럼 **누가 무엇을 하는
  장면인지**로 지시한다

```
❌ 이 영화의 사운드 디자인은 어떤 효과를 냈나요?
   → 용어를 그대로 물음. 어휘 없는 사람은 답 자체가 불가능
❌ 이 소설의 시점 전환은 어떤 효과를 냈나요?
   → 책도 마찬가지다. "시점"은 회원들이 쓰지 않는 말
❌ 일상적인 소리를 증폭해 불쾌감을 만드는데, 어떻게 느끼셨나요?
   → 답을 질문 안에 넣음. 토론이 확인 의식이 됨
✅ 귀를 막고 싶었던 순간을 떠올려 보세요. 그때 화면에 있던 건 대개 평범한
   것들입니다 — 새우 껍질, 주사기, 관절. 감독은 왜 이 소리들의 볼륨을 올렸을까요?
```

### 3–4) `thematic`

`angle` 허용값: `인물의 선택` / `작품의 입장` / `제목·상징` / `구조가 만든 효과`

- **두 문항의 `angle`이 달라야 한다.** 강제하지 않으면 모델이 같은 질문을 문장만
  바꿔 두 번 쓴다. 이 슬롯의 유일한 실패 모드이고, 반드시 일어난다
- **답이 갈려야 한다.** 4명이 같은 답을 할 질문이면 실패. 진영이 생기는 형태가 좋다
- 예/아니오로 끝나면 안 된다

```
❌ 이 작품은 무엇에 대한 이야기인가요?        → 요약 요청. 토론 아님
❌ 주인공의 선택은 옳았나요?                 → 예/아니오
✅ 칭린은 진실을 파헤치고 어머니는 잊는 쪽을 택했습니다.
   이 소설은 둘 중 어느 편에 서 있습니까?
```

### 5) `stance`

여섯 중 가장 위험한 슬롯. **nullable** — 팩트시트 단계에서 검증 가능한 입장이
없다고 판정되면 `null`, `thematic` 하나로 대체한다.

- **정치 라벨을 모델 기억에서 가져오지 않는다.** `source`는 `in_work`(작품 내부 증거)
  또는 `factsheet`(검증된 발언) 둘 중 하나여야 하고, 비어 있으면 생성 실패로 처리
- **의도 맞히기가 아니라 실행과 배신을 묻는다.** 작가가 그렇게 말했다고 작품이
  그렇게 된 것은 아니다. 재밌는 건 언제나 그 틈이다
- 억지로 채우지 않는다. 강제하면 없는 정치색을 지어낸다

```
❌ 감독은 마르크스주의자입니다. 이 영화가 자본주의를 어떻게 비판하나요?
   → 전제를 다 깔아주고 확인만 시킴. 4명이 같은 답을 들고 옴
✅ 이 영화는 여성의 몸을 소비하는 시선을 비판합니다. 그런데 그 비판을 위해
   카메라는 두 시간 동안 바로 그 시선을 그대로 재현합니다.
   극장을 나설 때 우리가 한 건 비판이었나요, 구경이었나요?
```

**위치가 5번인 것은 설계다.** 앞에 두면 이후 모든 대답이 그 프레임으로 수렴하고
2~4번이 각주로 전락한다. 각자 본 것을 다 꺼낸 다음에 던져야 뒤집힌다.

### 6) `private_experience`

- **작품 얘기를 하지 않는다.** 여기서 작품으로 되돌아가면 `thematic` 재탕이 된다
- "무엇을 배웠나요"는 금지. 교훈 요구는 종례지 토론이 아니다
- 강도 조절. 4인 모임에는 안 하고 넘어갈 자유가 없다. 답하기 싫으면 넘길 수 있는
  무게여야 한다

```
❌ 이 작품을 통해 무엇을 배우셨나요?          → 자기계발서 마무리
❌ 인생에서 가장 후회되는 일은 무엇인가요?     → 도망갈 구멍이 없음
✅ 우리 집에도 아무도 먼저 꺼내지 않는 이야기가 있나요?
   굳이 캐묻지 않는 건 배려일까요, 회피일까요?
```

---

## 3. 요소 프레임워크

### 영화 — 6요소

전부 **감독의 선택** 관점. "관객이 뭘 느꼈나"가 아니라 "감독이 뭘 골랐나".

| `element`             | 요소          | `observation`에 담을 것                                            | 질문 씨앗                                                                               |
| --------------------- | ------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| `mise_en_scene`       | 미장센        | 인물이 프레임 어디에 놓였나, 빈 공간에 뭐가 들어찼나, 색·조명·소품 | "인물이 화면 한쪽에 밀려 있는 장면이 많습니다. 반대쪽 여백은 무엇으로 채워져 있었나요?" |
| `editing`             | 편집          | 어떤 숏 다음에 어떤 숏이 붙었나, 컷이 빨라지는 구간                | "A장면 다음에 곧바로 B장면이 옵니다. 이 둘을 붙여놓아 생기는 의미는?"                   |
| `camera_movement`     | 카메라 무빙   | 핸드헬드 vs 고정·스테디캠, 카메라가 따라가나 기다리나              | "흔들리는 장면과 고정된 장면을 나눠보면, 어느 쪽에 무엇이 놓여 있었나요?"               |
| `sound_design`        | 사운드 디자인 | 증폭된 소리, **지워진 소리**, 침묵이 깔린 지점                     | "귀를 막고 싶었던 순간, 화면에 있던 건 대개 평범한 것들입니다. 왜 볼륨을 올렸을까요?"   |
| `directing_actor`     | 배우 연출     | 같은 배우의 다른 작품과 다른 점, 감정을 표정으로 주나 몸으로 주나  | "이 배우가 감정을 드러내는 방식이 유난히 절제돼 있습니다. 감독은 왜 그렇게 시켰을까요?" |
| `narrative_structure` | 서사 구조     | 사건이 놓인 순서, 시점 이동, 심어둔 복선                           | "이 사건을 맨 앞에 놓았습니다. 시간순이었다면 무엇이 달라졌을까요?"                     |

**미끄러짐 경고 — 프롬프트에 반드시 명시할 것**

- 사운드 디자인 ≠ 음악. **음악이 하나도 없는 영화도 이 항목에서 만점이 가능하다**
- 배우 연출 ≠ 연기력. 잘했냐가 아니라 감독이 뭘 끌어냈냐
- 서사 구조 ≠ 스토리. 내용이 아니라 **전달 순서**

**영화 전용 규칙**

- `observation`은 **장면을 특정할 수 있게** 써야 한다. "중반부의 어떤 장면"으로는
  네 사람이 서로 다른 장면을 떠올린 채 대화하게 된다. 기억력의 문제가 아니라
  지시의 문제다 — 인물·장소·행동 중 둘 이상을 명시할 것
- 영화는 `locator`를 쓸 수 없다. 책은 대목을 가리켜 그 자리에서 펼칠 수 있지만
  영화는 장면을 발제문에 실을 방법이 없으므로, `observation` 문장 자체가 좌표
  역할까지 해야 한다

### 책 — 5요소

전부 **작가의 선택** 관점.

| `element`          | 요소            | `observation`/`locator`에 담을 것                            | 질문 씨앗                                                                         |
| ------------------ | --------------- | ------------------------------------------------------------ | --------------------------------------------------------------------------------- |
| `voice_pov`        | 시점과 화자     | 누가 말하나, 몇 인칭인가, 화자가 아는 것과 모르는 것의 경계  | "이 이야기를 다른 인물이 말했다면 무엇이 달라졌을까요?"                           |
| `order`            | 순서            | 무엇을 언제 알려주나, 끝까지 안 알려준 것                    | "작가가 끝까지 밝히지 않은 것이 있습니다. 알려줬다면 어떻게 달라졌을까요?"        |
| `summary_scene`    | 요약과 장면     | 몇 년이 한 문단으로 지나간 곳 vs 몇 분이 여러 쪽을 차지한 곳 | "3년이 한 문장으로 지나가고 하루가 스무 쪽입니다. 왜 여기서 속도를 늦췄을까요?"   |
| `detail_selection` | 묘사의 취사선택 | 이례적으로 자세히 묘사된 사물, 통째로 생략된 것              | "이 장면에서 유독 자세히 묘사된 건 사람이 아니라 사물입니다. 왜 그쪽을 봤을까요?" |
| `dialogue_silence` | 대화와 침묵     | 대답하지 않은 질문, 말 대신 놓인 행동                        | "이 대화에서 한 사람은 끝내 대답하지 않습니다. 그 침묵은 무엇을 하고 있나요?"     |

**문장·문체는 의도적으로 제외했다.** 번역서에서는 작가가 아니라 옮긴이를 평가하게
되기 때문이다. 한국어 원작이라도 `element`로는 쓰지 않는다.

**책 전용 규칙**

- `locator`(대목 지시)는 필수다. 원문 인용도, 쪽수도 넣지 않는다. "누가 무엇을
  하는 대목"으로 가리키면 회원들이 그 자리에서 기억을 맞출 수 있다
- 영화보다 유리한 점은 남아 있다. 책은 회원이 책을 들고 오면 그 대목을 실제로
  펼칠 수 있지만, 영화는 장면을 다시 볼 방법이 없다

### 비문학 — 별도 세트

craft 요소가 적용되지 않는다. 논증 축으로 대체한다.

1. 저자의 핵심 주장
2. 근거의 종류 (일화 / 통계 / 사례)
3. 가장 약한 고리
4. 저자가 다루지 않은 반론
5. 저자의 이해관계

비문학에서 `stance`는 nullable이 아니라 **필수**다. 입장이 곧 본체이기 때문.

---

## 4. 자기검증 (4단계)

생성된 6문항을 저가 모델로 채점. 하나라도 걸리면 재생성, 재생성도 실패하면
`FAILED` 상태로 전환하고 소유자에게 수정 폼을 띄운다.

| #   | 검사                                                  | 대상  |
| --- | ----------------------------------------------------- | ----- |
| 1   | 예/아니오로 답이 끝나는가                             | 전체  |
| 2   | 답이 질문 안에 들어 있는가                            | 전체  |
| 3   | 사전 준비(세기·찾기·확인)를 요구하는가                | 전체  |
| 4   | 기법 용어가 질문문에 노출됐는가                       | 2번   |
| 5   | **줄거리 요약만 읽고도 쓸 수 있는 `observation`인가** | 2번   |
| 6   | `locator`에 원문 인용이나 쪽수가 섞였는가             | 2번   |
| 7   | `angle`이 겹치는가                                    | 3·4번 |
| 8   | `source`가 비어 있는가                                | 5번   |

**5번이 핵심이다.** 모델은 화면 수준 관찰이 없을 때 줄거리로 우회해서 기법 질문인
척하는 문장을 만든다. 검사 문장은 이것 하나면 된다 — _"이 `observation`은 작품을
보지 않고 줄거리 요약만 읽어도 쓸 수 있는가?"_ 쓸 수 있으면 실패다.
5번에 걸리면 재생성이 아니라 `technique`을 `null`로 떨어뜨린다.
